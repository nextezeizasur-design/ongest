// app/director/results/page.tsx
// Reutiliza la página de resultados del coordinator — el componente ya
// admite el rol 'director' (requireRole(['director','coordinator']))

export { default } from '@/app/coordinator/results/page'
